### Beekeeb Toucan

## Build firmware

Build both canonical halves, verify the generated configuration and DTS, copy the UF2s to `firmware-out`, and print their SHA-256 hashes:

```sh
./build-firmware.sh
```
